import TopBar from '../../components/TopBar/TopBar';

const MainLayout = () => {
  return (
    <div className="w-screen h-screen">
      {/* Header */}
      <TopBar />
    </div>
  );
};

export default MainLayout;
